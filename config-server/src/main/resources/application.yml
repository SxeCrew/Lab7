server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SxeCrew/Lab7-config
          default-label: main
          clone-on-start: true
          search-paths: config
  application:
    name: config-server